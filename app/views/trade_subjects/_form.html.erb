<%= form_for(@trade_subject) do |f| %>
  <% if @trade_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trade_subject.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @trade_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <span class="col-sm-12 col-md-3 col-md-offset-1">
      <%= f.label :'Name' %>
      <%= f.text_field :name %>
    </span>
    <span class="col-sm-12 col-md-3">
      <%= f.label :'Identification number' %>
      <%= f.text_field :identification_number %>
    </span>
    <span class="col-sm-12 col-md-3">
      <%= f.label :"VAT" %>
      <%= f.text_field :vat %>
    </span>
    <span class="actions">
      <%= f.submit %>
    </span>
  </div>
<% end %>