<%= form_for(@outbill) do |f| %>
  <% if @outbill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@outbill.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @outbill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
