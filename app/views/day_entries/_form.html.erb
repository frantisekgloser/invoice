<%= form_for(@day_entry) do |f| %>
  <% if @day_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@day_entry.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @day_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :'Day entry date' %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :'Day entry from' %><br>
    <%= f.time_select :start %>
  </div>
  <div class="field">
    <%= f.label :'Day entry till' %><br>
    <%= f.time_select :stop %>
  </div>
  <div class="field">
    <%= f.label :'Invoiced?' %><br>
    <%= f.check_box :invoiced %>
  </div>
  <div class="field">
    <%= f.label :'Task' %><br>
    <%= f.collection_select(:task_id, current_user.tasks.all, :id, :name) %>
  </div>
  
  <br>
  <br>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
