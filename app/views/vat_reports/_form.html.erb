<%= form_for(@vat_report) do |f| %>
  <% if @vat_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vat_report.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @vat_report.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <span class="col-sm-12 col-md-2 col-md-offset-1">
      <%= f.label :'Number' %>
      <%= f.text_field :number, size: 8 %>
    </span>
    <span class="col-sm-12 col-md-3">
      <%= f.label :'Date' %>
      <%= f.date_select :date %>
    </span>
    <span class="col-sm-12 col-md-2">
      <%= f.label :'To pay' %>
      <%= f.text_field :to_pay, size: 8 %>
    </span>
    <span class="col-sm-12 col-md-2">
      <%= f.label :'To receive' %>
      <%= f.text_field :to_receive, size: 8 %>
    </span>
  </div>
  <br>
  <div class="row">
    <span class="col-sm-12 col-md-5 col-md-offset-1">
      <%= f.label :'Note' %><br>
      <%= f.text_area :note, size: '40x3' %>
    </span>
    <span class="actions">
      <%= f.submit %>
    </span>
  </div>
<% end %>