<%= form_for(@outgoing_invoice_sequence) do |f| %>
  <% if @outgoing_invoice_sequence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@outgoing_invoice_sequence.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @outgoing_invoice_sequence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
    <span class="col-sm-6 col-md-4">
      <%= f.label :'Outgoing invoices sequence' %>
      <%= f.text_field :name %>
    </span>
    <span class="col-sm-6 col-md-4">
      <%= f.label :'Note' %><br>
      <%= f.text_area :note, size: '40x3' %>
    </span>
    <span class="actions">
      <%= f.submit 'Update outgoing invoices sequence'%>
    </span>
  </div>
<% end %>