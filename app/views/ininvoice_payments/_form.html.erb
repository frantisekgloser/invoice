<%= form_for(@ininvoice_payment) do |f| %>
  <% if @ininvoice_payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ininvoice_payment.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @ininvoice_payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <span class="col-sm-6 col-md-2">
      <%= f.label :'Amount' %>
      <%= f.text_field :amount %>
    </span>
    <span class="col-sm-6 col-md-2">
      <%= f.label :'Payment' %>
      <%= f.collection_select(:payment_id, current_user.payments.all, :id, :amount) %>
    </span>
    <span class="col-sm-6 col-md-2">
      <%= f.label :'Incomming invoice' %>
      <%= f.collection_select(:ininvoice_id, current_user.ininvoices.all, :id, :number) %>
    </span>
    <span class="col-sm-6 col-md-2">
      <%= f.label :'Exchange rate' %>
      <%= f.collection_select(:exchange_rate_id, current_user.exchange_rates.all, :id, :rate) %>
    </span>
    <span class="actions">
      <%= f.submit %>
    </span>
  </div>
<% end %>