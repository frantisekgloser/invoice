<%= form_for(@province) do |f| %>
  <% if @province.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@province.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @province.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :'Name' %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :'Country' %><br>
    <%= f.text_field :country_id %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
