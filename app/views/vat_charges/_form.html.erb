<%= form_for(@vat_charge) do |f| %>
  <% if @vat_charge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vat_charge.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @vat_charge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <span class="col-sm-6 col-md-4">
      <%= f.label :'Percentage' %>
      <%= f.text_field :percentage, size: 8 %>
    </span>
    <span class="col-sm-6 col-md-4">
      <%= f.label :'Country' %>
      <%= f.collection_select(:country_id, current_user.countries.all, :id, :name) %>
    </span>
    <span class="actions">
      <%= f.submit %>
    </span>
  </div>
<% end %>