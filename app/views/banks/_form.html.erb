<%= form_for(@bank) do |f| %>
  <% if @bank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bank.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @bank.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <span class="col-sm-4 col-md-3 col-md-offset-1">
      <%= f.label :'Bank' %>
      <%= f.text_field :name %>
    </span>
    <span class="col-sm-2 col-md-3">
      <%= f.label :'Code' %>
      <%= f.text_field :code %>
    </span>
    <span class="col-sm-3 col-md-3">
     <%= f.label :'SWIFT' %>
    <%= f.text_field :swift %>
    </span>
    <span class="actions">
      <%= f.submit %>
    </span>
  </div>
<% end %>