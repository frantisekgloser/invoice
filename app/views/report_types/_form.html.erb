<%= form_for(@report_type) do |f| %>
  <% if @report_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report_type.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @report_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <span class="col-sm-12 col-md-3 col-md-offset-1">
      <%= f.label :'Report type' %>
      <%= f.text_field :name %>
    </span>
    <span class="actions">
      <%= f.submit %>
    </span>
  </div>
<% end %>